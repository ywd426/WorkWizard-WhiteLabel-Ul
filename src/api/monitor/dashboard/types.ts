export interface ChartListVO {
  id: string;
  location: string;
  sensorId: string;
  chartOptions: string;
  translateLeft: number;
  translateTop: number;
  chartWidth: number;
  chartHeight: number;
  showMenu: boolean;
}
